// import axios from 'axios';

// const isClient = typeof window !== 'undefined';
// export const axiosClient = axios.create({
//  baseURL: 'http://localhost:8080',
//  headers: {
//  Authorization: isClient
//   ? `Bearer ${localStorage.getItem('__Pearl_Token') || ''}`
//   : '',
//  },
// });

